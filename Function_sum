def check_payment(A, B, C, D, X):
    total = (A * 1) + (B * 2) + (C * 5) + (D * 10)
    if total == X:
        print("Paid")
    elif total > X:
        print("Paid", total - X)
    else:
        print("Not Paid", total)
A, B, C, D = map(int, input().split())
X = int(input())

check_payment(A, B, C, D, X)
